import type { Metadata } from "next";
import { finds, money } from "@/lib/site-data";
export const metadata: Metadata = { title: "Garimpos", description: "Histórias completas de compras usadas, diagnóstico, reparo e resultado.", alternates: { canonical: "/garimpos" } };
export default function FindsPage() { return <main id="conteudo" className="page-main"><header className="page-hero"><span className="eyebrow-v2">GARIMPOS E REPAROS</span><h1>Do anúncio ao resultado.</h1><p>Preço, risco, defeito, custo e situação atual — sem esconder a parte que deu trabalho.</p></header><div className="finds-library">{finds.map((find, index) => <a className="find-card" href={`/garimpos/${find.slug}`} key={find.slug}><span className="find-index">0{index + 1}</span><div><span className="content-tag">{find.currentStatus}</span><h2>{find.product}</h2><p>{find.announcedProblem}</p></div><dl><div><dt>Preço pago</dt><dd>{money(find.negotiatedPrice)}</dd></div><div><dt>Reparo confirmado</dt><dd>{money(find.repairCost)}</dd></div></dl><i>Ver linha do tempo →</i></a>)}</div></main>; }
