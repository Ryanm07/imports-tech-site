import { requireChatGPTUser } from "@/app/chatgpt-auth";
import { AdminPanel } from "@/components/admin-panel";
export const dynamic = "force-dynamic";
export default async function AdminPage(){if(process.env.ADMIN_ENABLED!=="true")return <main id="conteudo" className="page-main"><div className="feature-soon"><span>ÁREA PROTEGIDA</span><h1>Painel administrativo desativado.</h1><p>A interface e as operações administrativas permanecem indisponíveis até a configuração explícita da feature flag e da lista de administradores.</p></div></main>; const user=await requireChatGPTUser("/admin"); const admins=(process.env.ADMIN_EMAILS||"").split(",").map((item)=>item.trim().toLowerCase()).filter(Boolean); if(!admins.includes(user.email.toLowerCase())) return <main id="conteudo" className="page-main"><div className="feature-soon"><span>ACESSO NEGADO</span><h1>Você não tem permissão.</h1><p>A autorização foi validada no servidor. Entre com uma conta administrativa.</p></div></main>; return <main id="conteudo" className="page-main"><header className="page-hero"><span className="eyebrow-v2">PAINEL ADMINISTRATIVO</span><h1>Conteúdo e moderação.</h1><p>Usuário autenticado: {user.email}. Todas as gravações passam por uma segunda validação no servidor.</p></header><AdminPanel/></main>}
